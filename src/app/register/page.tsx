'use client'; import Link from 'next/link'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import {createClient} from '@/lib/supabase/client';
export default function Register(){const [name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');const r=useRouter();async function go(e:React.FormEvent){e.preventDefault();const {data,error}=await createClient().auth.signUp({email,password,options:{data:{full_name:name}}});if(error)setMsg(error.message);else if(data.session)r.push('/dashboard');else setMsg('Account created. Check your email to confirm it.')}return <main className="grid min-h-[calc(100vh-70px)] place-items-center px-5"><form onSubmit={go} className="glass w-full max-w-md rounded-3xl p-8"><h1 className="text-3xl font-black">Create account</h1>{msg&&<p className="mt-4 text-sm text-cyan-200">{msg}</p>}<input required placeholder="Full name" value={name} onChange={e=>setName(e.target.value)} className="mt-7 w-full rounded-xl bg-white/5 border border-white/10 p-3"/><input required type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-4 w-full rounded-xl bg-white/5 border border-white/10 p-3"/><input required minLength={6} type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-4 w-full rounded-xl bg-white/5 border border-white/10 p-3"/><button className="mt-6 w-full rounded-xl bg-cyanx p-3 font-bold text-[#03101b]">Create account</button><p className="mt-5 text-center text-sm text-slate-500">Already registered? <Link className="text-cyanx" href="/login">Login</Link></p></form></main>}
