'use client'; import Link from 'next/link'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import {createClient} from '@/lib/supabase/client';
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');const r=useRouter();async function go(e:React.FormEvent){e.preventDefault();const {error}=await createClient().auth.signInWithPassword({email,password});if(error)setMsg(error.message);else { r.replace('/dashboard'); r.refresh(); }}return <main className="grid min-h-[calc(100vh-70px)] place-items-center px-5"><form onSubmit={go} className="glass w-full max-w-md rounded-3xl p-8"><h1 className="text-3xl font-black">Welcome back</h1>{msg&&<p className="mt-4 text-sm text-red-300">{msg}</p>}<input required type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-7 w-full rounded-xl bg-white/5 border border-white/10 p-3"/><input required type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-4 w-full rounded-xl bg-white/5 border border-white/10 p-3"/><button className="mt-6 w-full rounded-xl bg-cyanx p-3 font-bold text-[#03101b]">Sign in</button><p className="mt-5 text-center text-sm text-slate-500">No account? <Link className="text-cyanx" href="/register">Register</Link></p></form></main>}
